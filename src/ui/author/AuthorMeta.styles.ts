import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 'auto',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center'
  },
  imageContainer: {
    margin: 4
  },
  username: {
    color: '#5CB85C'
  },
  createdAt: {
    fontSize: 10,
    color: '#BBB'
  }
});
